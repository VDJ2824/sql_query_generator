# Legacy Implementations

This folder contains previous working implementations retained for reference during the migration.

Current contents:

- `fastapi-monolith/` contains the earlier FastAPI + SQLite + vanilla frontend test suite layout.

Do not delete this folder during migration. Move code out of it only when the replacement has been implemented, tested, and documented in the new architecture.
