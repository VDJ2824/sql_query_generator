# TLS Certificates

Place production TLS certificate files here when enabling HTTPS in `deploy/nginx/default.conf`.

Expected filenames:

- `fullchain.pem`
- `privkey.pem`

Do not commit real certificate private keys.
